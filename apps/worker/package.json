{
  "name": "@lex-os/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx watch src/main.ts",
    "lint": "eslint .",
    "start": "node dist/main.js",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --runInBand",
    "test:integration": "set -a; . ../../.env; set +a; NODE_ENV=test DATABASE_HOST=127.0.0.1 DATABASE_PORT=5433 REDIS_HOST=127.0.0.1 PROCESSING_QUEUE_PREFIX=lex-os-d7-worker-integration PROCESSING_STALE_AFTER_SECONDS=86400 PROCESSING_RECONCILE_INTERVAL_SECONDS=3600 PROCESSING_JOB_BACKOFF_MS=100 NODE_OPTIONS=--experimental-vm-modules jest --config test/jest.integration.config.cjs --passWithNoTests --runInBand",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@lex-os/config": "workspace:*",
    "@lex-os/contracts": "workspace:*",
    "@lex-os/database": "workspace:*",
    "@lex-os/shared": "workspace:*",
    "@nestjs/common": "11.1.28",
    "@nestjs/core": "11.1.28",
    "bullmq": "5.81.0",
    "reflect-metadata": "0.2.2",
    "rxjs": "7.8.2"
  },
  "devDependencies": {
    "@lex-os/eslint-config": "workspace:*",
    "@lex-os/tsconfig": "workspace:*",
    "jest": "30.4.2"
  }
}
